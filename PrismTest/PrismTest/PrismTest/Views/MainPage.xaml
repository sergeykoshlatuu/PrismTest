<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             xmlns:c1="clr-namespace:PrismTest.Converters"
             x:Class="PrismTest.Views.MainPage"
             Title="{Binding Title}" 
            >
    <ContentPage.Resources>
        <ResourceDictionary>
            <c1:ItemTappedEventArgsConverter x:Key="itemTappedEventArgsConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Button Text="Navigate to books page" Command="{Binding NavigateToBooksPageCommand}" />
        <Button Text="Navigate to authors page" Command="{Binding NavigateToAuthorsPageCommand}" />
        <Button Text="Navigate to publishers page" Command="{Binding NavigateToPublishersPageCommand}" />

    </StackLayout>

</ContentPage>